<%= flash_messages %>

<h1>Admin menu and Analytics</h1>

<h2>Average Mood Value</h2>
<%= area_chart @mood_ratings.group_by_week(:created_at).average(:value), min: 0, max: 1 %>


<h2>User registrations </h2>
<%= area_chart User.group_by_week(:created_at).count%>



<h3>There are a total of <%= User.count %> users registered.</h3>

<h2>Number of mood entries recorded </h2>
<%= area_chart @mood_ratings.group_by_week(:created_at).count, min: 0, max: 1 %>
<h3><%= @mood_ratings.count %> mood entries recorded so far.</h3>

<h3>We have proudly generated <%= Playlist.count %> playlists so far.</h3>